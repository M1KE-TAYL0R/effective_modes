pub struct Parameters {
    pub l_c: f64,
    pub c: f64,
    pub r: f64,
    pub w_0: f64,
    pub n_w: usize,
    pub n_q: usize,
    pub n_w_bins: usize,
    pub del_k: f64,
    pub quality: f64,
    pub q_range: (f64,f64),
    pub w_range: (f64,f64),
    pub routine: String,
}